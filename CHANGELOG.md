# Changelog

## [2026-03-13] - `unreleased`

### Fixed
- 修复正常 Stop 时直接清空系统代理而非恢复用户原始代理状态的问题，改为优先从恢复快照还原，失败才降级到基础清理。
- 修复 Stop 恢复快照失败后错误信息被无条件清空、用户无法在 UI 看到失败原因的问题。

### Changed
- 移除 `rollbackLocked` 已废弃的 `clearJournal` 参数，简化所有调用方。
- Start 中途失败的回滚路径现在也优先使用恢复快照还原网络状态，与 Stop 行为一致。
- `rollbackLocked` 每个清理步骤改为独立超时（DNS/代理清除 2s、TUN/快照恢复 5s），防止单步阻塞导致后续清理全部失败。
- macOS `ApplySystemProxy` 改为逐服务尝试，部分失败时跳过并继续，仅全部失败才报错。
- `SaveConfig` 在运行时活跃期间自动触发 400ms 防抖重启，快速连续变更只产生一次实际重启。
- 托盘标签根据运行状态动态显示模式（System/TUN）或空闲/异常状态。

### Added
- 新增 Start 失败后 rollback 走快照恢复路径的测试覆盖。
- 新增 Stop 恢复失败时错误码与错误消息保留的断言。
- 集成 Wails 3 构建系统，添加开发和生产环境资源文件系统实现（`appassets_dev.go`/`appassets_production.go`）。
- 配置 macOS 和 Windows 平台的 Taskfile 任务定义，支持一键构建、打包和分发。
- 新增 GitHub Actions 自动发布工作流，支持 macOS（amd64/arm64）和 Windows（amd64）多架构构建。
- 配置 NSIS 安装包模板用于 Windows 平台分发。
- 新增托盘"Repair Network"紧急按钮，无需打开主窗口即可强制恢复网络状态，运行中也可使用。
- 新增系统代理守护（Proxy Guard），运行中每 5s 校验系统代理是否被外部工具篡改，发现异常自动恢复并通知前端。
- 新增个人代理不可达时降级直连（Fail-Open），通过 `failOpenDirect` 配置开关控制（默认开启），恢复后自动切回；降级和恢复均推送事件通知前端。
- 新增系统代理守护、降级直连及通知去重的测试覆盖。
- 新增本地代理监听存活检测，运行中每 5s 探测 HTTP/SOCKS5 端口，失效时自动恢复网络状态并通知前端。
- 新增单实例保护，通过绑定固定端口 `:17899` 防止多实例同时运行；重复启动时自动激活已有窗口。开发模式可通过 `PROXYSEPARATOR_ALLOW_MULTI_INSTANCE` 环境变量跳过。

## [2026-03-12] - `unreleased`

### Fixed
- 修复个人代理长期占用系统代理时仍错误切到应用内 TUN 的问题，改为保持系统代理接管并叠加公司旁路路由。
- 修复公司域名命中 fake IP 或 IP 变更后仍可能沿用错误路由的问题，增加真实解析过滤与动态主机路由刷新。
- 修复 macOS TUN 启停异常后 DNS、路由或 `utun` 接口可能残留的问题。
- 修复前后端事件字段大小写或返回值包裹格式不一致导致运行时状态偶发不同步的问题。
- 修复运行中按钮因运行时事件、轮询结果和异常 payload 交错覆盖而在“启动隔离”与“关闭隔离”之间闪烁的问题。

### Changed
- 重构预检、启动和恢复流程：个人代理长期 TUN 模式下强制要求公司 CIDR 旁路规则，并将旁路接口与路由写入恢复快照以便自动清理。
- 重构 macOS TUN 启停链路为单次管理员提权脚本，统一处理 helper 启动、DNS 切换、路由安装、回滚和恢复，减少多次授权与残留风险。
- 优化前端运行时状态归一化与轮询逻辑，补充“个人代理长期 TUN”开关、默认 CIDR 规则和更稳定的启动/停止状态反馈。
- 调整 Wails 后端调用与窗口恢复行为，授权弹窗结束后会主动将主窗口带回前台。

### Added
- 新增“个人代理长期 TUN”模式，支持在 Clash / sing-box 等长期开启 TUN 的环境下通过系统代理入口 + 公司 CIDR 旁路运行。
- 新增公司域名动态解析拨号器，优先解析真实公司 IP，自动安装/刷新主机路由，并在缓存有效期内复用解析结果。
- 新增 `fix-network.sh` 网络恢复脚本，便于手动清理系统代理、DNS 和 `utun` 残留。
- 新增公司域名动态路由、个人 TUN 旁路预检与运行时清理的测试覆盖。

## [2026-03-11] - `780d689`

### Changed
- 增强前端运行时数据归一化逻辑，兼容日志、健康状态与 Wails 运行时事件的不同字段格式。
- 优化主界面状态展示，补充更稳定的日志渲染、状态标识和健康检查反馈。
- 调整后端启动流程与日志输出，提升启动、停止、恢复网络等操作的可观测性。

### Added
- 新增 `start.sh`、`stop.sh`、`restart.sh` 进程管理脚本，便于本地开发和调试。
- 新增前端运行时加载入口与 Wails 运行时类型声明，改善前后端桥接体验。
- 为上游健康检查、预检流程和运行时管理补充测试覆盖。

## [2026-03-11] - `9a05c63`

### Added
- 新增启动前检查（preflight）能力，在运行前校验上游代理、系统能力与目标模式可用性。
- 新增网络恢复能力与恢复日志（recovery journal），用于异常退出后的系统网络状态修复。
- 为 macOS、Windows 和其他平台补充模式探测与恢复相关实现。
- 前端新增预检结果展示、错误反馈和恢复网络入口。

### Changed
- 扩展运行时管理器，支持预检、恢复、模式回退及更完整的错误处理流程。
- 扩展 API 类型与绑定接口，向前端暴露预检报告和网络恢复操作。
- 更新配置存储与运行状态逻辑，以支持新的恢复和预检数据流。

## [2026-03-11] - `4c62908`

### Changed
- 大幅重构前端主界面，完善配置编辑、运行状态、流量统计、规则摘要、日志查看与路由测试体验。
- 重新设计样式系统，增强信息层次、交互反馈和整体可读性。

## [2026-03-11] - `d9864ef`

### Added
- 初始化 ProxySeperator 核心工程结构，建立 Go 后端与 Wails + React 前端的完整骨架。
- 新增配置模型、配置迁移与本地持久化存储。
- 新增规则解析与匹配引擎，支持域名后缀、完整域名、关键字和 CIDR 规则。
- 新增 HTTP / SOCKS5 上游代理转发、DNS 本地服务、流量统计与运行时管理模块。
- 新增 macOS / Windows 平台适配层，以及 TUN Helper 进程管理基础能力。
- 新增前端配置页、后端绑定、类型定义与基础样式。
- 新增后端、规则、运行时与配置相关测试。
- 新增产品、前端和后端技术设计文档。

## [2026-03-10] - `f58ad96`

### Added
- 初始化仓库。
- 新增项目设计文档 `PR.md`。
- 新增基础 `.gitignore`。
